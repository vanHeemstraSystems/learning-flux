-----

apiVersion: v1
kind: Service
metadata:
name: demo-app
namespace: default
labels:
app: demo-app
spec:
type: ClusterIP
ports:

- name: http
  port: 9898
  targetPort: http
  protocol: TCP
- name: grpc
  port: 9999
  targetPort: grpc
  protocol: TCP
  selector:
  app: demo-app
